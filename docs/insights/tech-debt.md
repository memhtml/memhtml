# memhtml-public · Tech debt

Aligned to the source at `410e36c`, version 0.15.1, on 2026-09-18. The previous alignment was `c3afc02` on 2026-09-11, and every measurement re-run for this pass carries the later date and sha rather than the earlier one.

This register was assembled from four channels, in this order.

First, a comment-marker scan. Two independent passes over the codebase found no debt markers, and the same pattern re-run on 2026-09-18 against the tree at `410e36c` returns seven hits, not one of which is a debt marker on this code, so this channel is still empty. The repo records debt in ranked prose ledgers instead of in code comments. `docs/backlog.md:3` states the rule for one of them, "Ranked. Each item names its evidence; nothing here is speculative," and `ROADMAP.md:3` names `docs/backlog.md` as "the fine-grained ledger" with itself as "the system-level view."

Second, those ledgers. `docs/backlog.md`, `ROADMAP.md`, `docs/bugs/`, and the EARS requirements in `spec/memhtml.symspec.json` each carry deferred items that name their own unfired trigger. Every ledger item that became a row below was re-checked against source before it was filed, and two were dropped because the code showed them already closed.

Third, manifest version pins. These cover the Effect pre-release catalog, the unstable subpath imports, the security override, and the cases where a declared schema version disagrees with the pinned version. One finding in this channel is a negative and belongs here rather than in a row, because there is no defect to rank: `packages/integrations`, the newest package in the tree, imports no `effect/unstable` subpath anywhere, and its one new exact pin, `jsonc-parser` at `3.3.1`, is declared twice at the same version (`packages/integrations/package.json:35` and `tests-integration/package.json:30`). Two same-version declarations of an exact pin are the shape row 17 flags with its numbers still in agreement, so this is a watch item, closed for good by moving the pin into the catalog so the two declarations cannot diverge. What the package does inherit is the catalog itself: it is the sixteenth manifest to declare `effect` as `catalog:` (`packages/integrations/package.json:34`), which widens the blast radius of row 9 without changing a word of it.

Fourth, pattern-level smells the reviewer chose to flag. These are duplicated agent-facing prose across the two doors, one oversized function, a phase whose destructive decision is never asserted, comment evidence citing a package the repo no longer depends on, and the ledger-outlives-its-defect pattern.

Category vocabulary is closed to eight values: `marker`, `wrong abstraction`, `error handling`, `dead code adjacent`, `deprecated pattern`, `version pin`, `duplicated logic`, `missing tests`. Cost is `S`, `M`, or `L`. Rank multiplies cost to fix by consequence of leaving, so a cheap fix with a real consequence outranks an expensive fix with a contained one.

A row leaves the register only by being closed in code, and it leaves through the **Closed** section below rather than by being deleted, which is the discipline `docs/backlog.md` keeps with its `~~struck~~ — DONE (verified <date>)` headings. The reason is the failure mode this register's own smell section names: a ledger that outlives its defect, and its mirror, a fix with no record that the ledger ever claimed otherwise.

Two limits apply to what follows. Every debt item in the register is either a deferred capability the maintainers already argued for in writing, or a smell this review found. Nothing was invented to reach a row count. The register also describes only this repository, which is the software that manages a separate `memhtml root` directory located by `$MEMHTML_ROOT`. No memory lives here. Where an item's consequence lands on the root rather than on the repo, the row says so.

## Ranked register

| Rank | Debt item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Category           | Cost to fix | Citation                                                                                                                                                                                                                         |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1    | The task family is CLI-only, so an agent holding only the MCP door cannot open, advance, or list a task. `setTaskStatus` and `listTasks` exist as use cases with a CLI adapter and no MCP adapter, against a layer whose rule is one adapter per tool.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | wrong abstraction  | M           | `docs/tasks.md:80-87`, `apps/cli/src/operations.ts:2171`, `apps/cli/src/operations.ts:2335`, `apps/cli/src/operations.ts:52-56`                                                                                                  |
| 2    | `memory_archive` moves a task into the archive without touching `task_status`, leaving a `todo`-status file under `archive/` in the root.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | wrong abstraction  | S           | `docs/tasks.md:87`                                                                                                                                                                                                               |
| 3    | The sleep `reprieve` phase archives TTL-passed memories out of the root, and no test asserts its `reprieved` or `expired` counts. Only the domain scoring function is covered.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | missing tests      | S           | `packages/sleep/src/phases/reprieve.ts:91`, `packages/domain/tests/retention.test.ts:371-424`                                                                                                                                    |
| 4    | `codeFor` has no `LlmContractViolation` arm while `messageFor` does, so a raised, tagged error reaches a caller as `code: "ERR_UNKNOWN"` carrying a perfectly specific human message. The switch has an arm for thirteen tags and a catch-all; the missing one is raised from two sites and declared as half of `LlmFailure`. That is the exact split `docs/design.md:360` forbids, because agents branch on `code` and never on the human `error` string, so no agent can branch on this failure at all.                                                                                                                                                                                                                                                                           | error handling     | S           | `apps/cli/src/errors.ts:41-79`, `apps/cli/src/errors.ts:115-116`, `packages/llm/src/structured.ts:137`, `docs/design.md:360`                                                                                                     |
| 5    | `packages/integrations/src/dialect.ts` has no test file of its own, and it is the module that decides whether an injected memory reaches the model at all. Three host-specific decisions are asserted only end to end through `apps/cli`: that a Cursor event other than `session-start` renders the empty string, that Codex gets the `hookSpecificOutput.additionalContext` shape, and that `cwdOf` prefers `cwd` over the first `workspace_roots` entry. A wrong dialect is silent by design, because every hook failure funnels to the empty string, so the failure mode is an integration that installs cleanly and injects nothing; `apps/cli/src/hook.ts:223` also uses the renderer as a capability predicate, so a dialect bug silently changes which hooks get installed. | missing tests      | S           | `packages/integrations/src/dialect.ts:17`, `packages/integrations/src/dialect.ts:33`, `packages/integrations/src/dialect.ts:62-69`, `apps/cli/src/hook.ts:25-28`, `apps/cli/src/hook.ts:223`                                     |
| 6    | `RECALL_DISCIPLINE_SHORT` has no consumer. Its own doc block says it exists to be appended to the retrieval tools' descriptions so an MCP-only agent still meets the two rules that most often go wrong, and three of the four surfaces the guidance module unifies do render the shared text while `apps/mcp/src/tools.ts` imports nothing from the module. An MCP-only agent therefore never sees the discipline the module was built to publish. `docs/insights/contract-map.md:1247` already records the same gap.                                                                                                                                                                                                                                                              | dead code adjacent | S           | `packages/contracts/src/guidance.ts:47-53`, `apps/cli/src/commands.ts:1540`, `packages/integrations/src/render/block.ts:59`, `docs/insights/contract-map.md:1247`                                                                |
| 7    | `mcpSuggestionsFor` is missing arms for four codes the MCP door can emit, so an agent gets an empty suggestion array where a human at the CLI gets a one-step recovery. `IndexStale` is the costly one: its whole published point is that the recovery is a re-index an agent can run itself, and it is a condition an agent hits on every read against a store whose index fell behind. `RebuildNoEmbedRefused`, `IntegrationModified` and `LlmContractViolation` are also absent. Nothing asserts the two arm sets are reconciled, so a new CLI arm silently never reaches the MCP door.                                                                                                                                                                                          | error handling     | S           | `apps/mcp/src/failure.ts:80-131`, `apps/cli/src/errors.ts:62-63`                                                                                                                                                                 |
| 8    | Agent-facing authoring and batching prose is hand-written twice, once per door, with no shared constant and no test asserting the two agree. An agent reading the CLI manifest and an agent reading `tools/list` can be told different rules. The shared-constant fix this row asks for now exists for a different body of prose, the recall discipline in `packages/contracts/src/guidance.ts`, and has not been extended to the authoring and batching strings.                                                                                                                                                                                                                                                                                                                   | duplicated logic   | M           | `apps/mcp/src/tools.ts:150-161`, `apps/mcp/src/tools.ts:208-215`, `apps/cli/src/commands.ts:1357-1406`, `packages/contracts/src/guidance.ts:18`                                                                                  |
| 9    | The MCP door runs on a pre-release Effect catalog pinned to `4.0.0-rc.117` and imports from the explicitly unstable `effect/unstable/ai` subpath in four source files plus two tests. The project's own notes say v4 breaks between versions, and nine of those notes (`CLAUDE.md:94`, `apps/mcp/src/server.ts:28`, `apps/mcp/src/tools.ts:49`, `apps/mcp/src/tools.ts:78`, `apps/mcp/src/tools.ts:93`, `apps/mcp/src/tools.ts:198`, `apps/mcp/src/tools.ts:373`, `apps/mcp/src/tools.ts:1034`, `docs/backlog.md:73`) still name `rc.109` as the version they were verified against, which is a large instance of the ledger-outlives-its-defect smell below. Sixteen manifests now ride the catalog, `packages/integrations` among them.                                           | version pin        | L           | `pnpm-workspace.yaml:93-96`, `apps/mcp/src/tools.ts:18`, `CLAUDE.md:92-94`                                                                                                                                                       |
| 10   | The conflict assist is write-time only. `memory_search` and `memory_recall` return a hit whose slot holds two live claims as though the fact were uncontested, even though `frame_key` is indexed and available.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | wrong abstraction  | M           | `docs/backlog.md:55`                                                                                                                                                                                                             |
| 11   | `integrations doctor` bounds the MCP handshake at 5000 ms with no retry and no third verdict between slow and broken, and reports the timeout as `A stdio server that has not answered by then is not wired`. The probe spawns a real `memhtml-mcp` child that opens the store, while the cheaper sibling probe, which opens nothing, is given twice the budget and says so. On a loaded machine the operator-facing result is `healthy: false` with a detail asserting a fact that is not the one measured, and the bound is untested because the suite drives both probes through the injected `DoctorProbes`.                                                                                                                                                                    | error handling     | S           | `packages/integrations/src/doctor.ts:71`, `packages/integrations/src/doctor.ts:74`, `packages/integrations/src/doctor.ts:197-204`, `packages/integrations/src/doctor.ts:573`, `packages/integrations/tests/doctor.test.ts:66-69` |
| 12   | `mount.test.ts` asserts a snapshot leak by censusing `tmpdir()` for a shared prefix, and `reachability.test.ts` creates two directories under exactly that prefix in the same shared `tmpdir()` in the same vitest project. If the two files overlap, the census sees a count the other file's fixtures changed and fails with a message about a leak that does not exist. The fix is a per-file suffix on the prefix, or a dedicated temp root for the census test. This is the tmpdir race between `mount.test.ts` and `reachability.test.ts` rather than a new defect in either.                                                                                                                                                                                                 | missing tests      | S           | `apps/consolidator/tests/mount.test.ts:457-486`, `apps/consolidator/tests/reachability.test.ts:264`                                                                                                                              |
| 13   | Counted-drop sites record how many records were unusable and never why, so an operator reading `skipped` or `droppedLines` cannot distinguish a truncated write from a schema change.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | error handling     | S           | `packages/sleep/src/phases/state-export.ts:120-122`, `packages/traces/src/extract.ts:254-258`                                                                                                                                    |
| 14   | A store created before migration 0009 carries NULL `frame_key` until `memhtml index rebuild` runs, so the conflict assist silently finds fewer conflicts on an older root.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | deprecated pattern | S           | `docs/backlog.md:69`, `ROADMAP.md:35`                                                                                                                                                                                            |
| 15   | `docs/code-mode.md` ships its cookbook helper against cheerio, which is not a dependency of any manifest and is known to be unloadable in the sandbox the recipes now run in. The doc's own caveat contradicts its code.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | dead code adjacent | S           | `docs/code-mode.md:20-35`, `ROADMAP.md:144`                                                                                                                                                                                      |
| 16   | `apps/cli/guest/corpus.mjs` is 193 lines of runtime-loaded source that `tsc` never sees, so its imports, its `atob` shim, and its selector contract carry no type check. biome does lint it — `files.includes` names `**/*.mjs` and the root `lint:repo` task runs `biome check .` — which catches syntax and unused bindings but not a shape disagreement with the corpus it parses. _judgment-call_                                                                                                                                                                                                                                                                                                                                                                               | missing tests      | S           | `apps/cli/src/exec.ts:95-107`, `biome.json:5-13`, `package.json:15`                                                                                                                                                              |
| 17   | The biome version is declared three ways again, and the two that agree agree on a version no manifest carries. `biome.json:2` names the `2.5.10` schema and `mise.toml:68` says "the pinned 2.5.10" in prose, while all seventeen manifests pin `@biomejs/biome` at `2.5.13`, so the schema a maintainer's editor validates against is not the version the binary runs. `packages/integrations/package.json:38` is the seventeenth manifest and is new since `c3afc02`. This reopens the biome entry in **Closed** below.                                                                                                                                                                                                                                                           | version pin        | S           | `biome.json:2`, `package.json:34`, `mise.toml:68`, `packages/integrations/package.json:38`                                                                                                                                       |
| 18   | `MCP_CANDIDATES` is a verbatim second copy of the CLI's sibling-path walk: the same two strings in the same order, resolved against `import.meta.url` in the same kind of loop, documented with the same reasoning in both places. The comment names the duplication rather than removing it, which is the honest form and still leaves two arrays a packaging change has to edit in step. If a build ever emits `memhtml-mcp.js` instead of `.mjs`, `memhtml serve` and `integrations install` break in different ways and only one of them is covered by a test. The dependency direction is the reason the copy exists, so the fix is to move the array into `@memhtml/contracts` and have both import it.                                                                       | duplicated logic   | S           | `packages/integrations/src/locate.ts:22-30`, `apps/cli/src/serve.ts:54`                                                                                                                                                          |
| 19   | Eight doc-comment citations point into `node_modules/eve/dist/...`, one with an exact line number, and eve has been in no manifest since commit 40511b7 replaced it with an in-process AI SDK tool loop. No install can materialize the cited files, so none of the eight is checkable by anyone, and each describes a dependency the consolidator no longer has.                                                                                                                                                                                                                                                                                                                                                                                                                   | dead code adjacent | S           | `apps/consolidator/src/mount.ts:18`, `apps/consolidator/src/contract.ts:698`, `scripts/probe-sandbox-egress.mjs:135`                                                                                                             |
| 20   | Two live comments in `apps/consolidator/src/output-budget.ts` still reason from `apps/consolidator/src/run-auth.ts`, a file commit 40511b7 deleted: one says nitro bundles this module into the agent alongside `mount.ts` and `run-auth.ts`, the other says it follows the same rule those two follow. This is the eve smell's exact shape, evidence naming something no install produces, applied to a first-party file instead of a third-party one, and `docs/architecture/module-map.md:118` still lists the deleted file as a live 231-LOC module.                                                                                                                                                                                                                            | dead code adjacent | S           | `apps/consolidator/src/output-budget.ts:10`, `apps/consolidator/src/output-budget.ts:58`, `docs/architecture/module-map.md:118`                                                                                                  |
| 21   | Any new MCP tool must declare `failure: ToolFailure` or its errors reach the agent as "internal server error". The handler side is now type-checked, because `kit.toLayer` requires `ToolFailure` as every handler's error type and a handler that fails with a raw domain error is a compile error. The tool-declaration side is still convention and wire tests only, so a tool registered without the declaration typechecks and masks every failure it raises.                                                                                                                                                                                                                                                                                                                  | deprecated pattern | M           | `docs/backlog.md:74`, `apps/mcp/src/handlers.ts:69`, `apps/mcp/src/tools.ts:47-55`                                                                                                                                               |
| 22   | The discrimination gate's fixture is saturated at MRR 1.0 with zero inversions over 36 probes, so it can detect a ranking regression and can no longer measure an improvement.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | missing tests      | M           | `ROADMAP.md:180-182`                                                                                                                                                                                                             |
| 23   | Sleep-cycle watermarks live only in `index.db`, which is a rebuildable projection. Losing it loses every watermark, and the next cycle re-reads and re-distills those sessions.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | wrong abstraction  | M           | `ROADMAP.md:208`                                                                                                                                                                                                                 |
| 24   | `AcceptanceStage` restates the domain's merge vocabulary as a flattened third copy, hand-fusing `MergeStage` and `MergeVetoReason` with nothing type-linking them, so adding a fourth divergence predicate compiles cleanly while the acceptance arm silently stops naming it. The arm's own doc block claims the opposite, that the stages are read off the filter's outcome rather than re-derived. A third copy hand-writes the stage literals in an `if/else` chain instead of exhausting `MergeStage`, so a new stage falls through to no counter with no compiler complaint. The fourth site, `VETO_WORDING` at `packages/sleep/src/phases/dedup-merge.ts:643`, is keyed by the domain type and is the pattern the other three should follow.                                 | duplicated logic   | M           | `packages/eval/src/write-path-acceptance.ts:39-47`, `packages/domain/src/merge.ts:198`, `packages/domain/src/merge.ts:228`, `packages/sleep/src/phases/dedup-merge.ts:602-605`                                                   |
| 25   | `packages/index/src/retrieval.ts:133` cites a line that moved: it attributes the `--dense` null-stripping to `apps/cli/src/envelope.ts:139`, and `stripNulls` is now declared at `apps/cli/src/envelope.ts:176` while line 139 is unrelated. Same failure mode as the eve citations, an unverifiable line-numbered claim, except the target is first-party and one grep away, so the fix is mechanical. The sibling disagreement is in the ledgers: DOCSYNC-1's note cites `apps/cli/src/envelope.ts:67` for `ERROR_CODES` while `docs/design.md:360` cites `:66`.                                                                                                                                                                                                                  | deprecated pattern | S           | `packages/index/src/retrieval.ts:133`, `apps/cli/src/envelope.ts:176`                                                                                                                                                            |
| 26   | Three doc comments count their own constants wrongly. `packages/html/src/vocabulary.ts:35` says "Each of the five is a fact no pure function can invent" over a `REQUIRED_META` that holds four entries. `apps/mcp/src/tools.ts:58` says "The eight types an agent may write" over a literal set that enumerates nine. `apps/cli/src/doctor.ts:36` says "Ten checks" over a numbered list that runs to eleven, the eleventh being vector coverage. Each is the ledger-outlives-the-defect shape at comment scale: a hand-typed count beside a constant it does not derive from.                                                                                                                                                                                                     | marker             | S           | `packages/html/src/vocabulary.ts:35`, `packages/html/src/vocabulary.ts:41-46`, `apps/mcp/src/tools.ts:58`, `packages/contracts/src/types.ts:42-52`, `apps/cli/src/doctor.ts:36`                                                  |
| 27   | `DuplicateContent` is declared, coded, messaged and suggested, and never raised. It has a `codeFor` arm, a `messageFor` arm, a CLI suggestion and an MCP suggestion promising that nothing was written and there is nothing to clean up, and every construction of it in the tree is in a test. A dedupe is instead reported as a success with `deduped: true`, which is the right behavior and makes the whole error path dead. Four maintained sites exist for an error nothing raises, and because `ERROR_CODES` is append-only the code itself cannot be removed, so the fix is to delete the arms or to record why the code is reserved.                                                                                                                                       | dead code adjacent | S           | `packages/contracts/src/errors.ts:44`, `apps/cli/src/errors.ts:56-57`, `apps/mcp/src/failure.ts:99-103`, `apps/mcp/src/tools.ts:214`                                                                                             |
| 28   | `batchWrite` runs 202 lines in one function body and carries two coordinate systems at once: the caller's op index space and the store's result positions, bridged by an `originOf` array. Row 29 is the same shape in the integrations package. _judgment-call_                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | wrong abstraction  | M           | `apps/cli/src/operations.ts:882-1083`, `apps/cli/src/operations.ts:932`                                                                                                                                                          |
| 29   | `planInstall` is a 183-line function of the shape row 28 flags in `batchWrite`: its body runs from its declaration to the next top-level one inside a 909-line file, and it carries two coordinate systems at once, planned writes keyed by path and entries keyed by managed role or claim. It is the only place host detection, renderer selection, editor selection, receipt drift reading and backup naming meet. It is driven directly by its own test suite, so the cost of leaving it is comprehension rather than correctness, which is exactly how row 28 is scoped. _judgment-call_                                                                                                                                                                                       | wrong abstraction  | M           | `packages/integrations/src/install.ts:454`, `packages/integrations/src/install.ts:637`, `packages/integrations/tests/transaction.test.ts:640`                                                                                    |
| 30   | An `article_html` op reports no conflicts at all, because the claim lives inside the caller's markup and the ops layer never parses it. Fixing it means moving the assist behind the store's own render.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | wrong abstraction  | L           | `docs/backlog.md:57`                                                                                                                                                                                                             |
| 31   | The salience read rule is argued from mechanism and never measured. No benchmark in the campaign exercises salience, because every row runs against a fresh store with no repeated access.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | missing tests      | L           | `ROADMAP.md:80`, `spec/memhtml.symspec.json` requirement GYM-1                                                                                                                                                                   |
| 32   | The MCP server cannot publish server-level `instructions`, because the dependency declares the field and gives `layerStdio` no argument for it. Tool descriptions are the only guidance channel an agent gets.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | version pin        | L           | `apps/mcp/src/server.ts:25-38`, `docs/backlog.md:73`                                                                                                                                                                             |

## Closed

Each entry names the row it retires, the date the closure was verified against source, and the code that settles it. An entry stays here rather than being deleted, so a reader who finds the same shape again knows it was already argued once.

- **~~The doc-sync ledger entry is stale in both directions~~** — DONE (verified 2026-08-18, re-checked 2026-09-18). `docs/backlog.md:27-29` carries the closure. `RESPONSE_TYPES` now holds 41 members (`apps/cli/src/envelope.ts:12-58`, the `RESPONSE_TYPES` array), five more than at the last alignment: `integrations.report`, `integrations.list`, `integrations.doctor`, `integrations.shell` and `hook.output`, appended with a comment at `apps/cli/src/envelope.ts:49-52` explaining why `hook.output` is declared for a command whose success stdout is not this envelope. `docs/design.md:360` still cites the enum by path rather than enumerating members, so that line still cannot drift from the constant and the closure stands. The ledger record of the closure has drifted again, which is the smell rather than the defect: `docs/backlog.md:29` still says the enum holds 32 members and points the by-path fix at line 878 of `docs/design.md`, a line that does not exist because that file is 407 lines long, and `ROADMAP.md:221` still says "32 members today". The register's own **A ledger that outlives the defect it describes** smell below is the general case, and this entry is now its live instance twice over, once for the defect it closed and once for its own record of the closure.
- **~~Three-way disagreement about the biome version~~** — DONE (verified 2026-08-25). `biome.json:2` declared the `2.5.8` schema, all fifteen manifests pinned `@biomejs/biome` at `2.5.8`, and `mise.toml` named no version in prose, so there was one number in one shape. Reopened 2026-09-11 and widened by 2026-09-18: the manifests are now seventeen and pin `2.5.13`, while `biome.json:2` stayed at `2.5.10` and `mise.toml:68` names `2.5.10` in prose, so the schema and the prose agree with each other on a version no manifest carries. That is register row 17.
- **~~`apps/cli/guest/corpus.mjs` is linted by nothing~~** — DONE (verified 2026-08-25), for the lint half only. `biome.json`'s `files.includes` names `**/*.mjs` and the root `lint:repo` task runs `biome check .` over `biome.json`'s own include set rather than each package's `src tests` (`package.json:15`), so every `.mjs` in the repo is checked. The type-check half stands as register row 16: `tsc` still never sees this file.
- **~~The consolidator's agent sandbox has full network egress and this repo cannot turn it off~~** — DONE (verified 2026-09-11, re-checked 2026-09-18). Commit 40511b7 removed eve, and the consolidator now runs an in-process AI SDK tool loop over three bounded read tools with no shell (`apps/consolidator/src/tools.ts:8-36`); `apps/consolidator/src/contract.ts:9` states that the contract module holds no eve import and no network call. `dangerouslyAllowFullInternetAccess` survives in three places: `scripts/probe-sandbox-egress.mjs`, which probes the old construction, `apps/cli/tests/exec.test.ts:532-538`, which asserts that `memhtml exec` does not set it, and the comment at `apps/cli/src/exec.ts:297`, which still describes eve's sandbox in the present tense. `ROADMAP.md:206` still lists the item as open.
- **~~`pinCorpusSnapshot` is implemented and tested but wired to nothing~~** — DONE (verified 2026-09-11, re-checked 2026-09-18). `apps/cli/src/exec.ts:504-525` acquires it through `Effect.acquireRelease` around `runExec` and releases it on failure, timeout and interrupt, which is the per-run scope the row said the object lacked. `apps/consolidator/src/client.ts:169` records that `memhtml exec` pins the snapshot, and `apps/consolidator/src/mount.ts:331` is the definition.

## Explicit markers

The codebase contains no explicit debt markers. The register's first assembly ran two independent passes (the two named in the opening paragraph), and the same marker pattern was run again on 2026-09-18 against the tree at `410e36c`; no pass found a marker comment in source:

- A `grep -rnEw` over the source tree on disk, covering `apps`, `packages`, `tests-integration`, `scripts`, `spec`, `mise-tasks` and `.github` and excluding `node_modules`, `dist`, `dist-check`, `.lighthouseci`, `.astro` and `coverage`, matching `\b(TODO|FIXME|HACK|XXX|REFACTOR|DEPRECATED|deprecated|@deprecated)\b`, returned exactly seven hits, and not one of them is a debt marker on this code. `packages/domain/src/merge.ts:39` is a doc comment that quotes `"deprecated"` as one of the rare load-bearing words a variant qualifier can be. `packages/domain/src/merge.ts:135` holds the string `"deprecated"` as a data member of `VARIANT_QUALIFIERS`. `packages/eval/eval/write-path-acceptance.test.ts:120` is a fixture id inside an expectation, `stageOf("k05/use-the-deprecated-sync-flag")`. `packages/eval/src/write-path-acceptance-corpus.ts:432` is corpus prose naming the variant veto, `packages/eval/src/write-path-acceptance-corpus.ts:440` is the fixture path `areas/cli/use-the-deprecated-sync-flag.html`, and `packages/eval/src/write-path-acceptance-corpus.ts:441` is that fixture's claim text. `.github/workflows/release-please.yml:42` is a YAML comment that uses the phrase "the deprecated" about a third-party action's Node runtime, not about anything in this repository. Five of the seven are new since `c3afc02`, and all five are fixture data from the `packages/eval` write-path acceptance arm rather than notes on code.
- Run over `packages/integrations` alone, the largest addition since the last alignment at 27 source files, the same pattern returns zero hits, so the whole integrations family introduces no marker.
- With `.lighthouseci` left in scope, the same pattern also matches every `lhr-*.html` and `lhr-*.json` Lighthouse report under `apps/docs/.lighthouseci/`. That directory is gitignored build output rewritten by each Lighthouse run, so a file count there is not a measurement this document can carry from one alignment to the next, and its contents are neither source nor in scope for this document.

No `@deprecated` decorator, no `// DEPRECATED` comment, and no `// REFACTOR` comment exists anywhere in the in-scope source.

Written ledgers replace markers here, and each ledger states its own rules:

- `` - `Ranked. Each item names its evidence; nothing here is speculative.`: `docs/backlog.md:3` ``
- `` - `Every item below names its evidence — a measured number, a probe, or a commit — and nothing here is speculative. Ranked within each horizon. `docs/backlog.md` remains the fine-grained ledger; this file is the system-level view the benchmarks bought.`: `ROADMAP.md:3` ``

Because of that practice, deferred work is written as a named decision with a named trigger instead of an inline note. Three examples, quoted verbatim:

- `` - `Two things remain open and are accepted rather than fixed:`: `ROADMAP.md:203` ``
- `` - `**Step 5, prompt caching, is deliberately NOT built, and its trigger is recorded.**`: `ROADMAP.md:199` ``
- `` - `What remains open is the watermark's coverage under reconsolidation: losing `index.db` loses every watermark, and the next cycle re-reads and re-distills those sessions.`: `ROADMAP.md:208` ``

An earlier draft of this register quoted a fourth, the `pinCorpusSnapshot` lifetime note in `apps/consolidator/src/client.ts`. That comment went away when the snapshot was wired in, which is the **Closed** entry above, and its trigger firing is what the practice is for.

The same practice appears in the EARS requirements carrying `status: draft` in `spec/memhtml.symspec.json`. The spec holds 107 requirements at `410e36c`, 94 `implemented`, 10 `draft` and 3 `approved`. The draft count is unchanged from the last alignment and the denominator moved from 101 because the integrations family added six requirements, `INT-1` through `INT-6` (`spec/memhtml.symspec.json:24`, `:43`, `:63`, `:83`, `:102`, `:122`), every one of them `implemented` rather than deferred. The ten drafts are `ANN-1`, `BACKFILL-1`, `CACHE-1`, `CODE-3`, `GYM-1`, `HOP-2`, `PERF-1`, `PROX-1`, `PROX-2` and `TASK-1`, and each has a `verificationNote` naming its unfired condition. Two verbatim examples:

- ``- `The mcp server shall expose the task family as task_add, task_status, and task_list tools.`: `spec/memhtml.symspec.json` requirement TASK-1``
- ``- `While a corpus approaches one hundred thousand chunks, the retrieval shall serve vector search through an approximate nearest neighbor index.`: `spec/memhtml.symspec.json` requirement ANN-1``

A third, DOCSYNC-1 ("The design doc shall derive the response-type list from the enum."), stood here in an earlier draft and is now `status: implemented` with a `verificationNote` dated 2026-08-26. It is the first **Closed** entry above, and the two ledger entries behind it describe a defect the code no longer has.

## Pattern-level smells

### One workflow, two hand-written copies, one per agent door

The two surfaces an agent calls, the `memhtml` CLI and the MCP stdio server, publish the same authoring and batching rules as two independently written prose strings. Both tell the caller to supply exactly one of `body` or `article_html`. Both state that the markup must carry exactly one `<mark>` inside the first `<p>` or `<li>`. Both explain that the first `<time datetime>` element becomes the memory's event time, which is what the recency arm ranks by. Both describe a fenced code block becoming `<figure><pre><code data-lang>` and promoting to a `lang:` entity. Both state that a batch is atomic by default with a flag for best-effort, and that a dedupe is not a failure. Neither string is derived from the other. Inside the MCP package the risk is already handled. `apps/mcp/src/tools.ts:194-195` says two hand-written versions of one workflow "drift the first time the semantics move", which is why `ARTICLE_HTML_CONTRACT` and `BATCH_GUIDANCE` are shared constants. That sharing does not extend past the package edge: a grep for either name under `packages/` returns nothing, so no shared module holds this prose. A grep for `BATCH_GUIDANCE` across every test directory finds one prose mention and no assertion, so the only drift test that exists checks that the MCP constant appears in MCP descriptions, and it cannot notice the CLI drifting. The consequence lands on agents rather than on maintainers. Two agents holding two different doors to the same root can operate from two different contracts, and neither can tell.

What changed since the last alignment is that the fix this section prescribes has been built, for a different body of prose. `packages/contracts/src/guidance.ts` is the shared constants module in `@memhtml/contracts` the cost line below asks for, and it holds the recall discipline rather than the authoring and batching rules: `apps/cli/src/commands.ts:1532-1541` documents exactly the reasoning given here, that four hand-written copies of one discipline would drift the first time a rule moved, and `packages/integrations/src/render/block.ts:59` and `packages/integrations/src/render/skill.ts:58` render the same paragraphs into the installed instruction block and the installed `SKILL.md`. So the pattern is adopted and the smell is unchanged: the authoring and batching prose is still hand-written twice, at `apps/mcp/src/tools.ts:150-161` and `:208-215` against `apps/cli/src/commands.ts:1357-1406`, and extending the module to cover it is now a smaller job than it was, because the module exists and three surfaces already read from it. Register row 6 is the other half of the same story: the one surface the guidance module cannot reach is the MCP door, because `apps/mcp/src/tools.ts` imports nothing from it.

Shows up in:

- `apps/mcp/src/tools.ts:150`
- `apps/mcp/src/tools.ts:208`
- `apps/cli/src/commands.ts:1357-1406`
- `apps/cli/src/commands.ts:243-247`
- `tests-integration/tests/batch.test.ts:43`
- `packages/contracts/src/guidance.ts:18`

Cost: M. The fix is to extend the shared constants module in `@memhtml/contracts` that now holds the recall discipline to hold the authoring and batching prose too, with both doors importing it, plus one test asserting that the strings the two doors publish are the same string.

### Comment evidence that cites a package the repo no longer depends on

The comments in this codebase carry their own evidence, in the form of measured numbers, named probes, and dated verifications. The problem is where some of that evidence lives. Eight doc-comment citations point into `node_modules/eve/dist/...`: four in `apps/consolidator/src/mount.ts` (lines 18, 45, 68 and 160), two in `apps/consolidator/src/contract.ts` (lines 698 and 778), one in `apps/consolidator/tests/mount.test.ts:230`, and one in `scripts/probe-sandbox-egress.mjs:135`. One carries an exact line number, the `JsonObject` citation at `apps/consolidator/src/contract.ts:698`, and none points into `eve/docs`. Commit 40511b7 replaced eve with an in-process AI SDK tool loop and deleted `apps/consolidator/src/run-auth.ts` and `apps/consolidator/agent/agent.ts`, which held most of the earlier citations, so eve now appears in no manifest: `apps/consolidator/package.json` declares seven dependencies, `@ai-sdk/amazon-bedrock` 5.0.81, `@ai-sdk/anthropic` 4.0.52, `@memhtml/contracts` as a workspace link, `ai` 7.0.93, `effect` from the catalog, `just-bash` 3.4.2 and `zod` 4.5.4. That changes the shape of the smell. Before the commit, `node_modules/` being the first line of `.gitignore` meant a fresh clone could not check the citations until it installed. After it, no install produces the cited files at all, so the eight comments are unverifiable by anyone and describe internals of a package that no longer runs here. The version stamps confirm the drift: `apps/consolidator/src/contract.ts:651` says its claim was measured on "eve 0.33.0" and `scripts/probe-sandbox-egress.mjs:27` reasons from "eve 0.31.0", and neither version is installed. The one security-relevant conclusion these comments used to carry, that the sandbox had full egress, is settled by the same commit and sits in **Closed** above; what remains is explanatory prose whose evidence a reader has to take on faith.

The same shape now has a first-party instance, which is register row 20. `apps/consolidator/src/output-budget.ts:10` and `:58` both reason from `apps/consolidator/src/run-auth.ts`, the file the same commit deleted, so two live comments explain a bundling rule and a dependency-freedom rule by pointing at a module no checkout contains. A third-party citation at least names something a reader could once have installed; a first-party one names a path a reader can prove is absent in a second, which makes it the cheaper half of this smell to close.

Shows up in:

- `apps/consolidator/src/mount.ts:18`
- `apps/consolidator/src/mount.ts:45`
- `apps/consolidator/src/mount.ts:68`
- `apps/consolidator/src/mount.ts:160`
- `apps/consolidator/src/contract.ts:698`
- `apps/consolidator/src/contract.ts:778`
- `apps/consolidator/tests/mount.test.ts:230`
- `scripts/probe-sandbox-egress.mjs:135`
- `apps/consolidator/src/output-budget.ts:10`
- `apps/consolidator/src/output-budget.ts:58`

Cost: S. Each comment either restates its claim against `just-bash`, which is still a real dependency whose declarations an install does produce, or drops the citation and keeps the conclusion with a date. A version stamp naming a package that is not installed is worse than no stamp. The `run-auth.ts` pair is cheaper still, because the replacement fact is in the same file.

### A ledger that outlives the defect it describes

Written ledgers replace comment markers here, and they share one failure mode with markers. An entry can stay in the ledger after its defect is gone. The doc-sync item is the pattern's settled instance, closed 2026-08-18 (`docs/backlog.md:27-29`, and the **Closed** section above). It said `design.md`'s ResponseType line names 16 of 31 members, `ROADMAP.md:221` repeats it, and DOCSYNC-1 in `spec/memhtml.symspec.json` carried it as a draft requirement with the same count in its verification note. A direct check of the source disagreed on both counts. `RESPONSE_TYPES` holds 41 members, and `docs/design.md:360` cites the constant by path instead of listing anything, so a grep for any response-type literal in that document returns zero. The entry was wrong about the enum's size and wrong that the defect exists. The wider problem is that three documents agreed on a number none of them re-derived, which is the same shape as the selector error `ROADMAP.md:145` records, where "three documents agreed on the wrong selector; the markup settled it."

The strongest instance the pattern now has is the closure record itself. `docs/backlog.md:29` and `ROADMAP.md:221` both still state that the enum holds 32 members, a number that was already wrong at the last alignment and is now nine short, and `docs/backlog.md:29` additionally points the by-path fix at line 878 of `docs/design.md`, in a file that is 407 lines long. So the ledger entry that records the closing of a count-drift defect has itself drifted on a count and on a line number, which is a sharper example of the failure mode than any of the originals.

Two other entries drift the same way to a smaller degree. `docs/code-mode.md:31-35` still presents cheerio as the cookbook's parser after `ROADMAP.md:144` measured that cheerio cannot load in the sandbox the recipes now run in. And nine prose sites each name Effect `4.0.0-rc.109` as the version their claims were verified against while `pnpm-workspace.yaml:93` pins `rc.117`: `CLAUDE.md:94`, `apps/mcp/src/server.ts:28`, `apps/mcp/src/tools.ts:49`, `:78`, `:93`, `:198`, `:373`, `:1034` and `docs/backlog.md:73`. That is nine documents agreeing on a version the catalog left eight release candidates ago, and the count grew rather than shrank between alignments.

The pattern also runs at comment scale, which is register rows 25 and 26. Three doc comments state a count of a constant they sit beside and do not derive from, and all three are now wrong (`packages/html/src/vocabulary.ts:35`, `apps/mcp/src/tools.ts:58`, `apps/cli/src/doctor.ts:36`), and one doc comment cites a first-party line that moved (`packages/index/src/retrieval.ts:133`, naming `apps/cli/src/envelope.ts:139` for a function now declared at `:176`). A hand-typed count and a hand-typed line number are the same defect as a ledger entry that outlives its item: a claim nothing re-derives.

Shows up in:

- `docs/backlog.md:27-29`
- `ROADMAP.md:221`
- `apps/cli/src/envelope.ts:12-58`
- `docs/design.md:360`
- `docs/code-mode.md:31-35`
- `CLAUDE.md:94`
- `packages/html/src/vocabulary.ts:35`
- `apps/mcp/src/tools.ts:58`
- `apps/cli/src/doctor.ts:36`
- `packages/index/src/retrieval.ts:133`

Cost: S per instance. DOCSYNC-1 already names the fix that closes the whole class, which is to derive the doc line from the enum the way `AGENTS.md` is derived from the command table.

### A destructive sleep phase whose decision is never asserted

The sleep cycle's `reprieve` phase decides whether a TTL-passed memory earns another two weeks or gets archived out of the root, and it reports that decision as a count of what it reprieved and what it expired. No test asserts either number. The scoring function underneath it is well covered, with property tests for monotonicity, importance ordering, and the negative-outcome clamp at `packages/domain/tests/retention.test.ts:394`, so the arithmetic is covered. The phase body is not. `tests-integration/tests/sleep.test.ts:168` asserts `report.phases` has `SLEEP_PHASES.length` entries — against the enum rather than a literal, so adding a phase cannot leave an integration file asserting a stale count — and then inspects `retention-triage` and `report` by name. A grep for `reprieved` or `expired` across every sleep test and every integration test finds no assertion on either field. `packages/sleep/tests/run.test.ts:404` names reprieve only in prose, as one of eleven predecessors that ran before the phase under test. The result is that the one phase that deletes a user's memories from the managed root passes the definition-of-done gate with no check that it archived what it should have or spared what it should have.

This package also has the weakest coverage ratio in the repo. The ratios below were counted at `410e36c` on 2026-09-18 by counting `.ts` files under each package's `src/` directory against its `tests/` directory, which is a measurement a reader can re-run rather than a number this page holds on its own authority, and stating the method is the point: five hand-counted ratios that nothing derives are the same defect register row 26 catches in three doc comments and the ledger smell catches in `docs/backlog.md:29`. `sleep` is 38 source files against 19 test files. `eval` is 14 against 6 under `tests/`, with three more gate files under `eval/` that the method above does not count. `llm` is 10 against 7 and `mcp` is 7 against 5. The new `packages/integrations` is 27 against 15, about 1.8 to 1, which puts a fifth package in the near-2:1 band and retires the claim that every other package sits near 1:1; one of the source files there with no test file of its own is `packages/integrations/src/dialect.ts`, which is register row 5.

Shows up in:

- `packages/sleep/src/phases/reprieve.ts:91`
- `packages/sleep/src/phases/reprieve.ts:11`
- `tests-integration/tests/sleep.test.ts:168`
- `packages/sleep/tests/run.test.ts:404`
- `packages/domain/tests/retention.test.ts:371-424`

Cost: S. The fix is one phase test with a TTL-passed high-salience memory and a TTL-passed cold one, asserting the counts and the resulting paths, and shown to fail when the reprieve predicate is inverted.

### An assist that only half the doors can use

Write-time conflict detection was the campaign's largest measured win, and it reaches callers unevenly. The prose door has it, so `memory_write_batch` and `memhtml apply --detect-conflicts` return a per-op `conflict` naming the colliding path, batch index, and claim. Three gaps remain, each documented and each with a different cause. An `article_html` op reports nothing at all, because the claim lives inside the caller's markup and the ops layer never parses it. An agent that authors markup to get event-time control gives up conflict detection in exchange. Retrieval also reports nothing, so a `memory_search` hit whose slot holds two live claims looks like one uncontested fact even though `frame_key` sits indexed on `files`. A root created before migration 0009 carries NULL `frame_key` until a rebuild, so the assist finds fewer conflicts there rather than wrong ones. All three fail toward under-reporting rather than false reports, which is why they were deferred. The cost of leaving them is that an agent's confidence in "no conflict reported" depends on which door it used and how old the root is, and nothing in the response tells it which situation it is in.

Shows up in:

- `docs/backlog.md:57`
- `docs/backlog.md:55`
- `docs/backlog.md:69`
- `ROADMAP.md:35`
- `apps/cli/src/operations.ts:545`

Cost: L for the `article_html` half, because the assist has to move behind the store's own render. Adding a second parse would add a second place the gist rule can drift. Cost is M for the retrieval half, and S for making the pre-0009 degradation visible in the response instead of only in a migration header.

## See also

- [memhtml-public · System overview](../architecture/system-overview.md): 6 shared source citations
- [memhtml-public · Processes](../behavior/processes.md): 7 shared source citations
- [memhtml-public · Dependency graph](../diagrams/structural/dependency-graph.md): 8 shared source citations as of this rewrite (that page's own See also line at `docs/diagrams/structural/dependency-graph.md:131` still says 2 and predates it); one of the eight, `apps/consolidator/agent/agent.ts`, is a file that document still cites at `docs/diagrams/structural/dependency-graph.md:95` and `docs/diagrams/structural/dependency-graph.md:122` after its deletion at 40511b7, and the same row pins eve at `0.38.3` against `apps/consolidator/package.json:42`, a manifest line that now closes the dependency block rather than declaring anything, with `just-bash` two lines above it
