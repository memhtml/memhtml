# The vocabulary this project writes in, and nothing else. A word belongs here when it is a
# term of art, an identifier quoted from source, or a dependency's name — never because a
# spelling was inconvenient to fix. This project writes US English: `cspell.json` carries a
# `flagWords` list that rejects the recurring British forms (`behaviour`, `colour`, …) with
# their US replacements.
#
# cspell splits on case and on `-`/`_`/`.`, so a compound like `rrfWeight` needs only `rrf`.

# --- the system's own nouns ---------------------------------------------------------------
memhtml
Memhtml
MEMHTML
rebuildable
Rebuildable
Rebuildability
refusable
greppable
browsable
inspectable
unopenable
unparseable
unplaceable
unsuffixed
vectorless
unitless
Unitless
Invertibility
injective
bitemporal
superlinearly
formedness

# --- retrieval and index vocabulary -------------------------------------------------------
embedder
dedup
Dedup
rels
Rels
RELS
metas
hrefs
pagerank
reproject
reprojects
reweight
rescan
rescans
rescanned
upserts
EWMA

# --- pipeline and operations vocabulary ---------------------------------------------------
oneline
sandboxed
unentitled
oncall
pgsql

# --- quoted from something outside this repo ----------------------------------------------
# `smali` is a language id the code-fence detector can return: `internals/the-memory-file-format`
# names it as the wrong answer a confidence score cannot rule out.
smali
# `déployé` is the French token `internals/four-arm-retrieval` uses to show that the tokenizer
# keeps a combining-accent word whole.
déployé
# `serch` is a deliberate misspelling: `learn/operations/diagnose-poor-retrieval` prints the
# `unknown command: serch x` envelope a mistyped invocation returns.
serch
# The test corpus's person names, quoted verbatim by `internals/the-memory-file-format` and
# `internals/the-sleep-pipeline` where they state the MEASURED character-overlap scores that make
# entity resolution's alias oracle necessary (`laith` against `laith al-saadoon` at 0.476). The
# numbers are only checkable against the names they were measured on, so the docs cannot paraphrase
# them into a generic `alice`/`bob` pair.
laith
saadoon
sanju
llms
Grayscale
